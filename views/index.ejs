<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/stylesheets/login.css">
    <script src="https://cdn.jsdelivr.net/npm/axios/dist/axios.min.js"></script>
    <title>Login</title>
</head>
<body>
    <% if (isAuthenticated && user['https://impulsewebapp.azurewebsites.net/roles'][0] === "Manager ") { %>
        <script>
            window.onload = function() {
                window.location.href = 'https://impulsewebapp.azurewebsites.net/ManagerHomepage.ejs';
            };
        </script>
    <% } %>
    <% if (isAuthenticated && user['https://impulsewebapp.azurewebsites.net/roles'][0] === "Employee") { %>
        <script>
            window.onload = function() {
                window.location.href = 'https://impulsewebapp.azurewebsites.net/EmployeeHomepage.ejs';
            };
        </script>
    <% } %>
    <% if (isAuthenticated && user['https://impulsewebapp.azurewebsites.net/roles'][0] === "Human Resources") { %>
        <script>
            window.onload = function() {
                window.location.href = 'https://impulsewebapp.azurewebsites.net/HRHomepage.ejs';
            };
        </script>
    <% } %>
        
    <section class="background-container"></section>
    <img class="logo" src="../assets/impulse-5-20-2024.png" alt="Logo" width="250" height="70">
    <section class="slogan">

        <p class="unite">unite.</p>
        <p class="collaborate">collaborate.</p>
        <p class="achieve">achieve.</p>
    </section>
    <button class="signIn"><a href="/login/">Login</button>
        <script src="../js_files/getUsers.js"></script>
</body>

<footer> impulse. est. 2024&trade;</footer>
</html>